apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cems-ingress
  namespace: cems-workloads
spec:
  ingressClassName: azure-application-gateway
  rules:
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: retail-service
            port:
              number: 80
